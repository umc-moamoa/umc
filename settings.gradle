pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        maven {url "https://jitpack.io"} //pie chart
        maven { url 'https://devrepo.kakao.com/nexus/content/groups/public/' } //kakao login
    }
}
rootProject.name = "umc_hackathon"
include ':app'
